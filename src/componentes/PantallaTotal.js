import React from 'react'
import '../hojas-de-estilo/pantallaTotal.css'

const PantallaTotal = ({ input }) => (
    <div className='input'>
       {'Total a pagar : '} {input} 
    </div>
); 

export default PantallaTotal